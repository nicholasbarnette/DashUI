import React from 'react';
import { Label } from '../../Label';

export default {
	component: Label,
	title: 'Label',
};

export const Basic = () => {
	return <Label id="label">Basic Label</Label>;
};
